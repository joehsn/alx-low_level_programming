#include "main.h"
#include <string.h>

/**
 * _strcmp - a function that compares two strings.
 *
 * @s1: a param
 *
 * @s2: a param
 *
 * Return: an intager
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
